# Configurable-Logging-and-Debugging-System-in-C
Developed a lightweight logging and debugging system using C and preprocessor directives. Supports ERROR, WARNING, INFO, and DEBUG levels with compile-time configuration. Displays timestamp, file, line number, and function details. Includes modular source files and Makefile support for Linux/GCC.
